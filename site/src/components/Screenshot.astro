---
const base = import.meta.env.BASE_URL;

const callouts = [
  { label: 'Hierarchy Tree', position: 'top-[15%] left-[3%]', color: 'blue' },
  { label: 'Property Viewer', position: 'top-[15%] right-[3%]', color: 'emerald' },
  { label: 'Level Filter & Search', position: 'top-[45%] left-[3%]', color: 'amber' },
  { label: 'Action Buttons', position: 'bottom-[15%] left-[3%]', color: 'violet' },
  { label: 'CSV Viewer & AWP 4D', position: 'bottom-[15%] right-[3%]', color: 'rose' },
];

const colorMap: Record<string, string> = {
  blue: 'bg-blue-500/90 border-blue-400',
  emerald: 'bg-emerald-500/90 border-emerald-400',
  amber: 'bg-amber-500/90 border-amber-400',
  violet: 'bg-violet-500/90 border-violet-400',
  rose: 'bg-rose-500/90 border-rose-400',
};
---

<section id="screenshot" class="py-24">
  <div class="mx-auto max-w-7xl px-6">

    <h2 class="section-heading">Plugin Interface</h2>
    <p class="section-sub">
      WPF 기반 통합 UI로 BIM 속성 관리와 4D 자동화를 하나의 화면에서 수행합니다
    </p>

    <!-- Screenshot with callouts -->
    <div class="relative mt-12 rounded-2xl border border-slate-200 bg-slate-50 p-2 sm:p-4 overflow-hidden dark:border-slate-800/80 dark:bg-slate-900/40">
      <img
        src={`${base}images/dxtnavis-main.png`}
        alt="DXTnavis Plugin Interface"
        class="w-full rounded-xl"
        loading="lazy"
      />

      <!-- Callout overlays -->
      {callouts.map((c) => (
        <div class={`absolute ${c.position} hidden lg:block`}>
          <span class={`inline-block rounded-full border px-3 py-1 text-xs font-medium text-white backdrop-blur-sm ${colorMap[c.color]}`}>
            {c.label}
          </span>
        </div>
      ))}
    </div>

    <!-- Feature highlights below screenshot -->
    <div class="mt-8 grid grid-cols-2 sm:grid-cols-5 gap-3">
      {callouts.map((c) => (
        <div class="rounded-lg border border-slate-200 bg-white px-3 py-2.5 text-center dark:border-slate-800/60 dark:bg-slate-900/40">
          <div class="text-xs font-medium text-slate-700 dark:text-slate-300">{c.label}</div>
        </div>
      ))}
    </div>

  </div>
</section>
